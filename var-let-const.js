let age = 24;
age = 25;

const countryName = 'Bangladesh';
// countryName = 'USA';
console.log(countryName);

/**
 * to declare a variable,
 * var --> same name can be redeclare multiple times without any error
 * let --> almost same as var but same named variable can be declare once
 * const --> unchangeable variable. only can be change or declare once
 */